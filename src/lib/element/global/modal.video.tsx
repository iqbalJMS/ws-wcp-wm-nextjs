'use client';
import React from 'react';

const ModalVideo = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      <div>{children}</div>
    </>
  );
};

export default ModalVideo;
